#include "main.h"
#include <stdio.h>
/**
* print_square - print square on screen
* @size: The size of square
* Return: void
*/
void print_square(int size)
{
int n, m;
if (size <= 0)
_putchar('\n');
for (a = 0; a < size; a++)
{
for (b = 0; b < (size); b++);
{
putchar('#');
}
putchar('\n');
}
}
